 Diagnóstico do problema
Quando você clica para adicionar o e-book interativo, a requisição não está chegando em uma API de backend real que devolva JSON.

Em vez disso, a resposta que você recebe é um HTML de erro do Vite/Replit (<!DOCTYPE html>) — ou seja, não existe uma rota configurada para receber essa chamada no ambiente de produção do Replit.

O problema real é:

A rota de API onde deveria salvar o e-book interativo não existe ou está quebrada no ambiente de deploy.

🛠 Causa raiz
Provavelmente o seu código frontend está fazendo algo como:

ts
Copiar
Editar
fetch('/api/disciplines/:id/ebooks', {...})
Só que o servidor do Replit (no ambiente de produção) não tem essa rota /api/disciplines/:id/ebooks implementada.

O Replit está tentando resolver o /api/... como se fosse um arquivo ou página estática, e te devolve um HTML de erro (do próprio Vite) em vez de um JSON da API.

✅ Solução definitiva
Você precisa criar no backend a rota correspondente para receber a requisição do e-book interativo.

1. Criar ou corrigir o endpoint no backend
Em server/api/ebooks.ts (ou similar):

ts
Copiar
Editar
import { serve } from "some-server-lib"; // Se for Deno ou Node

serve(async (req) => {
  const { pathname } = new URL(req.url);

  if (pathname.startsWith("/api/disciplines/") && pathname.endsWith("/ebooks")) {
    if (req.method === "POST") {
      const body = await req.json();
      // Aqui você salva no banco, ou apenas simula salvamento
      return new Response(JSON.stringify({ success: true, ebook: body }), {
        headers: { "Content-Type": "application/json" },
      });
    }
  }

  return new Response("Not Found", { status: 404 });
});
✅ Essa rota /api/disciplines/:id/ebooks precisa existir no servidor para responder corretamente.

2. Garantir que o fetch aponte para o domínio correto
Se o frontend estiver rodando em replit.dev, o fetch deveria chamar:

ts
Copiar
Editar
fetch('/api/disciplines/33/ebooks', {...})
ou

ts
Copiar
Editar
fetch(`${import.meta.env.VITE_BACKEND_URL}/api/disciplines/33/ebooks`, {...})
caso você esteja usando variáveis de ambiente.

📋 Resumo do que precisa ser feito:

Etapa	Ação
Backend	Criar a rota /api/disciplines/:id/ebooks
Backend	Garantir que a resposta sempre seja application/json
Frontend	Confirmar que a URL do fetch é correta e atinge a API de verdade
Frontend	Continuar renderizando o viewer (iframe, video, embed) conforme o tipo do link
🔥 Resumo técnico (em 1 linha)
O problema não é no frontend agora, é que o servidor não tem a rota /api/disciplines/:id/ebooks configurada.