“A resposta não é um JSON válido” ao tentar adicionar um e-book interativo com link externo (Drive) aponta claramente para uma falha no backend, que está retornando algo que não é um JSON válido (como por exemplo: HTML, texto puro ou até erro 404).

🛠 Diagnóstico com base nos prints:
🔴 Você está tentando salvar um e-book com link do Google Drive (ex: https://drive.google.com/file/d/...).

🔴 O console mostra:

plaintext
Copiar
Editar
Dados do e-book interativo processados: null  
message: 'Recurso não disponível'
Isso quer dizer que o servidor tentou processar o link enviado, mas não conseguiu entender ou acessar o recurso.

🔴 O erro final:

"Falha ao adicionar e-book interativo: A resposta não é um JSON válido"
significa que o backend não respondeu com application/json, o que quebra o fetch().json() do frontend.

✅ O que você precisa verificar no seu backend:
O controller da API que recebe os dados do e-book interativo está retornando res.send(...) com um HTML ou erro vazio?

Ele deve obrigatoriamente responder com:

json
Copiar
Editar
{ success: true, ebookId: 123 }
Ou em caso de erro:

json
Copiar
Editar
{ success: false, error: "Link inválido" }
O endpoint que trata POST /api/disciplines/:id/ebook existe na versão de produção?

Pode ser que em dev ele esteja implementado corretamente, mas não foi incluído no deploy de produção.

Teste manualmente no navegador ou via curl:

bash
Copiar
Editar
curl -X POST https://edunexia.replit.app/api/disciplines/17/ebook \
-H "Content-Type: application/json" \
-d '{"title": "Teste", "url": "https://drive.google.com/file/..."}'
Erro 404 retornando HTML em vez de JSON

Se a rota está quebrada ou mal configurada no router do servidor, o navegador está tentando fazer res.json(), mas recebe HTML de erro.

✅ Solução recomendada
No backend (em controllers/ebookController.ts ou onde estiver o handler), adicione uma verificação e sempre envie um JSON como resposta, até mesmo em erros:

ts
Copiar
Editar
try {
  // salvar e-book
  res.status(200).json({ success: true, message: 'E-book salvo com sucesso' });
} catch (err) {
  console.error(err);
  res.status(400).json({ success: false, error: 'Erro ao salvar o e-book' });
}
🔁 Próximos passos:
 Verifique se o link do Drive está acessível publicamente (alguns links não funcionam como download direto).

 Teste a rota de forma isolada via Postman ou curl.

 Garanta que o controller backend esteja retornando application/json corretamente.

 Veja se em fetch(...).then(r => r.json()) do frontend o erro não está vindo com text/html