import { Express } from "express";

/**
 * Função vazia para compatibilidade de código
 */
export function setupAuth(app: Express) {
  // Nenhuma autenticação implementada
  console.log("Sistema de autenticação removido");
}
